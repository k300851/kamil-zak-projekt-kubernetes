apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ticket-app
  labels:
    group: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
